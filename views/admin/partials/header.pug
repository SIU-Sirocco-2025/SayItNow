header.admin-header
    .container-fluid
        .d-flex.justify-content-between.align-items-center
            h1.h4.mb-0 Admin Dashboard

            //- Admin user info & logout
            if adminUser
                .dropdown
                    button.btn.btn-outline-primary.btn-sm.dropdown-toggle(
                        type="button"
                        data-bs-toggle="dropdown"
                        aria-expanded="false"
                    )
                        i.bi.bi-person-circle.me-1
                        = adminUser.fullName
                    ul.dropdown-menu.dropdown-menu-end
                        li
                            a.dropdown-item(href=`${prefixAdmin}/settings`)
                                i.bi.bi-gear.me-1
                                | Cài đặt
                        li
                            hr.dropdown-divider
                        li
                            a.dropdown-item.text-danger(href=`${prefixAdmin}/auth/logout`)
                                i.bi.bi-box-arrow-right.me-2
                                | Đăng xuất